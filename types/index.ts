export interface PostMetadata {
    title: string;
    date: string;
    subtitle: string;
    author: string;
    slug: string;
}

export interface ArcostData {
    left: string;
    middle: string;
    right: string;
}
